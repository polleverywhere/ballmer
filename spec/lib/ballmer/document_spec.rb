require 'spec_helper'

describe Ballmer::Document do
  subject { read_presentation('presentation3.pptx') }

  context "rels" do
    it "should resolve"
  end

  context "content_types"
  context "zip"
end
